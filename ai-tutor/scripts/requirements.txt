# RoboDynamics Content Validator — Python dependencies
# Install: pip install -r scripts/requirements.txt

pymysql>=1.1.0          # MySQL driver (pure Python, no binary dependency)
httpx>=0.27.0           # HTTP client for Claude API calls (already in tutor-api)
