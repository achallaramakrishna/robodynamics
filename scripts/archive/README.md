# Script Archive

Archived temporary and one-off root-level scripts are kept here instead of the repository root.

Current archive batch:
- `root-temp-2026-03-09`: 409 `tmp_*` shell, PowerShell, and Python scripts moved out of the root.

If a script becomes active again, move it back into a purpose-specific folder such as `scripts/production/`.
